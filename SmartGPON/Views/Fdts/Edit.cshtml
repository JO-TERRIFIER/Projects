@model SmartGPON.Web.ViewModels.FdtUpdateVM
@{ ViewData["Title"] = "Modifier FDT"; }

<link rel="stylesheet" href="~/css/gpon-modal.css" />

<div class="gpon-overlay">
  <div class="gpon-modal">
    <div class="gpon-modal-header">
      <h5><i class="bi bi-pencil me-2"></i>Modifier FDT</h5>
      <a href="@Url.Action("Index")" class="btn-close-modal">✕</a>
    </div>
    <div class="gpon-modal-body">
      <form asp-action="Edit" method="post">
        @Html.AntiForgeryToken()
        <input asp-for="Id" type="hidden" />
        <div class="row g-3">
          <div class="col-12">
            <label asp-for="Nom" class="form-label text-muted small mb-1">Nom <span class="text-danger">*</span></label>
            <input asp-for="Nom" class="form-control">
            <span asp-validation-for="Nom" class="text-danger small"></span>
          </div>
          <div class="col-12">
            <label asp-for="OltId" class="form-label text-muted small mb-1">OLT <span class="text-danger">*</span></label>
            <select asp-for="OltId" class="form-select">
              <option value="">— Sélectionner un OLT —</option>
              @if (ViewBag.Olts != null)
              {
                foreach (var o in (IEnumerable<SmartGPON.Core.Entities.Olt>)ViewBag.Olts)
                {
                  <option value="@o.Id">@o.Nom (@(o.Zone?.Nom ?? ""))</option>
                }
              }
            </select>
            <span asp-validation-for="OltId" class="text-danger small"></span>
          </div>
          <div class="col-md-6">
            <label asp-for="Latitude" class="form-label text-muted small mb-1">Latitude</label>
            <input asp-for="Latitude" class="form-control">
            <span asp-validation-for="Latitude" class="text-danger small"></span>
          </div>
          <div class="col-md-6">
            <label asp-for="Longitude" class="form-label text-muted small mb-1">Longitude</label>
            <input asp-for="Longitude" class="form-control">
            <span asp-validation-for="Longitude" class="text-danger small"></span>
          </div>
        </div>
        <div class="gpon-modal-footer">
          <a href="@Url.Action("Index")" class="btn-cancel">Annuler</a>
          <button type="submit" class="btn sg-btn sg-btn-primary">
            <i class="bi bi-check2 me-1"></i>Enregistrer
          </button>
        </div>
      </form>
    </div>
  </div>
</div>

@section Scripts {
  @if (!ViewData.ModelState.IsValid) {
    <script>document.querySelector('.gpon-modal')?.scrollIntoView({behavior:'smooth'});</script>
  }
}
