@model List<BpiDisplayVM>
@{ ViewData["Title"] = "BPIs"; }
<div class="page-header d-flex justify-content-between align-items-center">
  <h1 class="page-title"><i class="bi bi-boxes me-2"></i>BPIs</h1>
  @if (!User.IsInRole("Visiteur")) {
    <a asp-action="Create" class="btn btn-primary btn-sm"><i class="bi bi-plus-lg me-1"></i>Nouveau BPI</a>
  }
</div>
@if (!Model.Any()) {
  <div class="alert alert-info">Aucun BPI accessible.</div>
} else {
  <div class="card shadow-sm">
    <div class="table-responsive">
      <table class="table table-hover mb-0">
        <thead class="table-light"><tr>
          <th>Nom</th><th>FDT</th><th>Capacité</th><th>Lat/Lon</th><th class="text-end">Actions</th>
        </tr></thead>
        <tbody>
          @foreach (var b in Model) {
          <tr>
            <td class="fw-semibold">@b.Nom</td>
            <td>@b.FdtNom</td>
            <td>@b.Capacite</td>
            <td class="text-muted" style="font-size:12px">@(b.Latitude?.ToString("F4") ?? "—") / @(b.Longitude?.ToString("F4") ?? "—")</td>
            <td class="text-end">
              @if (!User.IsInRole("Visiteur")) {
                <a asp-action="Edit" asp-route-id="@b.Id" class="btn btn-sm btn-outline-secondary me-1"><i class="bi bi-pencil"></i></a>
                <form asp-action="Delete" asp-route-id="@b.Id" method="post" class="d-inline">
                  @Html.AntiForgeryToken()
                  <button type="submit" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button>
                </form>
              }
            </td>
          </tr>
          }
        </tbody>
      </table>
    </div>
  </div>
}
