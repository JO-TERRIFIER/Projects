@model ProjetCreateVM
@{ ViewData["Title"] = "Nouveau projet"; }
<div class="page-header"><h1 class="page-title"><i class="bi bi-folder-plus me-2"></i>Nouveau projet</h1></div>
<div class="card shadow-sm" style="max-width:600px">
  <div class="card-body">
    <form asp-action="Create" method="post">
      @Html.AntiForgeryToken()
      <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
      <div class="mb-3">
        <label asp-for="Nom" class="form-label fw-semibold">Nom du projet</label>
        <input asp-for="Nom" class="form-control" placeholder="Ex: Déploiement GPON Zone Nord" />
        <span asp-validation-for="Nom" class="text-danger small"></span>
      </div>
      <div class="mb-3">
        <label asp-for="ClientId" class="form-label fw-semibold">Client</label>
        <select asp-for="ClientId" class="form-select" asp-items="ViewBag.ClientsSelectList">
          <option value="">— Choisir un client —</option>
        </select>
        <span asp-validation-for="ClientId" class="text-danger small"></span>
      </div>
      <div class="mb-4">
        <label asp-for="Statut" class="form-label fw-semibold">Statut</label>
        <select asp-for="Statut" class="form-select">
          <option value="0">En cours</option>
          <option value="1">Terminé</option>
          <option value="2">Suspendu</option>
        </select>
      </div>
      <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary"><i class="bi bi-check-lg me-1"></i>Créer</button>
        <a asp-action="Index" class="btn btn-outline-secondary">Annuler</a>
      </div>
    </form>
  </div>
</div>
